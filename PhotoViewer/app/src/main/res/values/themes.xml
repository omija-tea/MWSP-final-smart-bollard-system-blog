<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PhotoViewer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PhotoViewer" parent="Base.Theme.PhotoViewer" />

    <!-- ActionBar가 있는 테마 (볼라드 제어 화면용) -->
    <style name="Theme.PhotoViewer.ActionBar" parent="Theme.Material3.DayNight">
        <!-- ActionBar 테마 설정 -->
    </style>
</resources>